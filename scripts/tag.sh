# 该命令运行前需要确认 user.name 和 user.email 是否已经设置
# git config --global user.email "you@example.com"
# git config --global user.name "Your Name"
git tag -a v1.1 -m "1.1 版本发布，变更内容见 CHANGELOG.adoc"
#git push origin --tags

