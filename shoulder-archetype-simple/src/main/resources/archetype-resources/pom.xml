<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.itlym</groupId>
        <artifactId>shoulder-parent</artifactId>
        <version>0.8.1</version><!-- shoulder-version -->
    </parent>

    <groupId>${groupId}</groupId>
    <artifactId>${artifactId}</artifactId>
    <version>${version}</version>
    <packaging>jar</packaging>

    <name>${artifactId}</name>
    <description>Demo project for Shoulder Framework</description>

    <properties>
        <java.version>17</java.version>
        <shoulder.version>0.8.1-SNAPSHOT</shoulder.version><!-- shoulder-version -->

        <errorCodePrefix>"0x000b"</errorCodePrefix><!-- todo 请确保每个应用错误码唯一 -->
    </properties>

    <dependencies>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

        <dependency>
            <groupId>cn.itlym</groupId>
            <artifactId>shoulder-starter-web</artifactId>
        </dependency>

        <!-- 数据库相关 -->
        <!--        <dependency>-->
        <!--            <groupId>cn.itlym</groupId>-->
        <!--            <artifactId>shoulder-starter-mysql</artifactId>-->
        <!--        </dependency>-->
        <!-- demo 工程，可使用 h2 链接 -->
        <!--        <dependency>-->
        <!--            <groupId>com.h2database</groupId>-->
        <!--            <artifactId>h2</artifactId>-->
        <!--        </dependency>-->
        <!-- 对象转换，若不习惯使用 SpringConvert，可选择 mapstruct -->
        <!--        <dependency>-->
        <!--            <groupId>org.mapstruct</groupId>-->
        <!--            <artifactId>mapstruct</artifactId>-->
        <!--        </dependency>-->
        <!--        <dependency>-->
        <!--            <groupId>org.mapstruct</groupId>-->
        <!--            <artifactId>mapstruct-processor</artifactId>-->
        <!--        </dependency>-->

        <!-- AOP操作日志 使用说明：https://github.com/ChinaLym/shoulder-framework/tree/master/shoulder-build/shoulder-base/shoulder-operation-log -->
        <!--        <dependency>-->
        <!--            <groupId>cn.itlym</groupId>-->
        <!--            <artifactId>shoulder-operation-log</artifactId>-->
        <!--        </dependency>-->

        <!-- 应用监控相关（附工具包：可监控的线程池） -->
        <!--        <dependency>-->
        <!--            <groupId>cn.itlym</groupId>-->
        <!--            <artifactId>shoulder-starter-monitor</artifactId>-->
        <!--        </dependency-->

        <!-- SpringBoot 热更新插件，修改代码无需重启 -->
        <!--        <dependency>-->
        <!--            <groupId>org.springframework.boot</groupId>-->
        <!--            <artifactId>spring-boot-devtools</artifactId>-->
        <!--            <scope>runtime</scope>-->
        <!--            <optional>true</optional>-->
        <!--        </dependency>-->

        <!-- =================================== test =================================== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <!-- 去掉不推荐的 junit4 -->
            <exclusions>
                <exclusion>
                    <groupId>org.junit.vintage</groupId>
                    <artifactId>junit-vintage-engine</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- SpringBoot 标准插件,默认由maven打成jar包运行-->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <!-- Shoulder 插件 自动生成错误码以及对应的翻译 说明文档  -->
            <plugin>
                <groupId>cn.itlym.shoulder</groupId>
                <artifactId>shoulder-maven-plugin</artifactId>
                <configuration>
                    <errorCodePrefix>${errorCodePrefix}</errorCodePrefix>
                    <scanPackage>${package}</scanPackage>
                </configuration>
            </plugin>

        </plugins>

        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <includes>
                    <include>**/*</include>
                </includes>
                <!-- 是否替换资源中的属性，properties.yml、banner 中需要获取应用版本号、名称、描述 -->
                <filtering>true</filtering>
            </resource>
        </resources>
        <!--单元测试时引用src/main/resources下的资源文件-->
        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
            </testResource>
            <testResource>
                <directory>src/main/resources</directory>
            </testResource>
        </testResources>

    </build>


</project>
