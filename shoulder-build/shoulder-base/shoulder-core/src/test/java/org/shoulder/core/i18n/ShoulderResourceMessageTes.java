package org.shoulder.core.i18n;

import org.apache.logging.log4j.message.Message;
import org.assertj.core.api.Assertions;
import org.junit.Test;
import org.shoulder.core.i18.ShoulderReloadableResourceBundleMessageSource;
import org.springframework.context.MessageSource;

import java.util.ArrayList;
import java.util.List;
import java.util.Locale;

public class ShoulderResourceMessageTes {

    /*@Test
    public void calculateAllFilenames_en(){
        ShoulderReloadableResourceBundleMessageSource messageSource = new ShoulderReloadableResourceBundleMessageSource();
        List<String> filenames = messageSource.calculateAllFilenames(null,new Locale("en",""));
        Assertions.assertThat(filenames).isNotEmpty();
    }

    @Test
    public void calculateAllFilenames_en(){
        ShoulderReloadableResourceBundleMessageSource messageSource = new ShoulderReloadableResourceBundleMessageSource();
        List<String> filenames = messageSource.calculateAllFilenames(null,new Locale("en",""));
        Assertions.assertThat(filenames).isNotEmpty();
    }*/

}
